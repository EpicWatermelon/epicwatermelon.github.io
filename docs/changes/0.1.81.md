# 0.1.81

## 建设进度

- 网站建设进度更新为 `50%`；英文初始标签、中文语言切换文案和进度条宽度同步为同一数值。

## 夜间舰队全景航程

- 夜间 01–05 的舰队航程由 `7.2s` 调整为 `9.6s`，月级巡洋舰以稳定速度从视口左侧画外飞至右侧画外。
- Hyperion 的斜向航线延伸到画面外左上方与右下方；淡入淡出缩短至航程两端的约十分之一，飞船进入画面后能完整呈现航迹。
- 新增回归断言，锁定慢速全宽航程、Hyperion 的完整对角线、短淡入淡出，以及减少动态时保持隐藏。

## 验证

- `npm test`：124 项全部通过。

## 夜景风速恢复

- 公开夜景的树、草与 Saber 待机动画恢复为每帧 `260ms`；日景继续使用 `180ms`，避免日景 Studio 的快节奏影响夜景。
- 夜间 Scene Studio 改用本地草稿键 `zhengji-scene-studio-night-v5`，让旧的 `180ms` 草稿不会覆盖恢复后的默认速度。
- 新增回归断言，锁定日景 `180ms`、夜景 `260ms` 的独立风速。

## 白天主题可读性

- 修复白天 00 首页底部的角色说明和“进入履历”提示被草地像素纹理干扰的问题：只将文字调整为纯白，维持夜晚版相同的无底框结构。
- 修复 01–05 白天章节导航仅设置父级颜色、链接自身仍沿用低透明度颜色的问题；导航链接改为深松绿，首页深蓝天空中保留高亮浅色版本。
- 新增回归测试，锁定首页提示与章节导航在白天主题下的显式高对比颜色规则。

- 日期：2026-08-02
- 状态：本地工作树
- 当前版本来源：`package.json`

## 白天场景默认组合

- 将确认的 Day Scene Studio 组合写入默认值：树、地基、草地、层级和金色树冠灯光保持用户给定的坐标与比例。
- 公开白天首屏保持 `tree-day-v2` 静态树与 `meadow-day-wind-v7`；Day Scene Studio 保留 `tree-day-wind-v9` 供手动比较，草地帧节奏设为 `180ms`。
- Day Studio 使用新的本地草稿键 `zhengji-scene-studio-day-v3`，避免浏览器中的旧 `Breezy / 260ms` 草稿覆盖定稿。
- CSS、i18n 与动态脚本缓存参数升级至 `0.1.81`，本地预览刷新后加载新配置。

## 验证

- 白天文字配色定向回归测试：1 项通过；首页辅助文字采用纯白，章节导航保留深松绿的高对比方案。
- `npm test`：117 项全部通过。
- `?studio=day` 刷新后显示 `Gusty` 与 `180ms`，并加载强风树冠和草地帧组。

## 日景树冠连续性

- 日景主页暂时保持单张 `tree-day-v2` 静态树图；当前 IMAGE2 的无蒙版候选会改变树形，未进入公开页面。后续精灵图必须经蒙版编辑锁定原树干、根系和主枝后才可替换。
- 新增回归测试，锁定日景树使用原始静态图且没有整树 CSS 位移。

## 日间树像素区位移实验

- 新增独立本地页面 `tree-wind-lab.html`，公开日间首页继续使用静态 `tree-day-v2`，两者互不影响。
- 从原始树图生成固定层、可动叶层与二值蒙版；固定层保留树根、主干和主要枝干，静止位置下两张彩色层可逐像素重建原图。
- 树冠按水平条带执行整数像素位移，默认最大摆幅 `3px`、条带高度 `8px`、循环时长 `3.6s`，相邻条带每帧最多相差 `1px`；每帧均从原始叶层重采样，避免累计拖影。
- 实验页提供摆幅、条带高度、循环时长、蒙版显示与暂停控制，并保留 `prefers-reduced-motion` 降级行为。
- 新增 `tools/build_tree_day_pixel_wind_layers.py`，可从 `tree-day-v2.png` 重建三张实验素材。

## 像素区位移实验验证

- `tests/tree-wind-lab.test.mjs`：3 项通过，覆盖页面结构、整数位移边界与循环连续性、分层互斥和静止帧无损重建。
- `npm test`：117 项全部通过，包含原有 114 项站点回归与新增 3 项实验页测试。
- 本地浏览器验证：Canvas 使用原图 `1672 × 941` 像素尺寸；运行状态下连续截图发生变化，暂停后连续截图完全一致；参数、蒙版与恢复默认值均可用，控制台无警告或错误。

## 日间树三层遮盖修正

- 保留上一版整数水平条带运动，停止二维位移场路线；公开日间主页继续使用原始静态树。
- 从 `tree-day-v2.png` 生成 `v2-back`、`v2-fixed`、`v2-front` 三张互斥素材，绘制顺序固定为后层深绿叶 → 树干与主要枝干 → 前层亮叶和枝梢。
- 后层收纳树干后方及与主树冠分离的深色叶团，共 `54,817` 个像素；后层使用前层 `40%` 的最大摆幅和双倍条带高度，维持受枝干遮蔽的轻微跟随。
- 前层保留上一版完整摆幅，固定层保持静止；三层在零摆幅时逐像素重建原始树图。
- “显示遮盖层级”使用青色、橙色和黄绿色分别标示后叶、固定枝干和前叶，并在控制面板明确列出由后到前的绘制顺序。

## 三层遮盖验证

- `tests/tree-wind-lab.test.mjs`：4 项通过，新增后叶运动幅度、固定枝干、绘制顺序、三层互斥、深绿叶归层和静止帧无损重建检查。
- `npm test`：118 项全部通过。
- 本地浏览器验证：后层青色蒙版受橙色树干遮盖，前层黄绿色蒙版位于最上层；运行截图发生变化，暂停后连续截图一致，控制台无警告或错误。

## 条带蓝色接缝修正

- 保留原版树冠的位移曲线、整数偏移、`3px` 默认摆幅、`8px` 前层条带高度和 `3.6s` 循环节奏。
- 每个移动条带在绘制时向上、向下各多采样 `1px`，相邻条带形成 `2px` 的纵向护边覆盖；护边与源像素保持同一纵坐标，只填补错位时露出的蓝天裂缝，不改变任何水平位移值。
- 后叶、前叶及层级蒙版共用同一护边绘制函数，固定树干与主要枝干仍保持原位。

## 接缝修正验证

- 新增护边回归测试，锁定上下采样范围、相邻重叠宽度、纵向对齐及原位移值不变。
- `tests/tree-wind-lab.test.mjs`：5 项全部通过；`npm test`：119 项全部通过。
- 本地浏览器使用 `6px` 最大摆幅进行压力检查，树冠近景没有出现贯穿条带边界的蓝色裂线；恢复 `3px` 后运动帧继续变化，暂停帧稳定，控制台无警告或错误。

## 树冠内部位移空腔修正

- 保留现有水平条带、护边、运动曲线和三层遮盖关系；新增 `tree-day-pixel-wind-v2-underfill.png`，绘制顺序调整为内部承托叶影 → 后层深绿叶 → 固定树干与主枝 → 前层亮叶和枝梢。
- 承托层只取自原始树冠中央区域已有的后叶与前叶，共 `188,812` 个像素；颜色压暗后形成内部叶影，静止帧被原叶片完整盖住，风动时填补叶片离开原位后露出的蓝天空腔。
- 承托层限制在树冠中央椭圆内，并在 `y = 700` 以上结束，避免外轮廓出现静止叶片残影；公开日间主页仍使用原始静态树。

## 内部空腔修正验证

- 新增回归测试，锁定承托层像素来源、内部范围、暗度及最底层绘制顺序；`tests/tree-wind-lab.test.mjs`：6 项全部通过。
- `npm test`：120 项全部通过。
- 本地浏览器以 `6px` 最大摆幅连续抽查三个风动相位，枝叶交界处由深绿色叶影承接，未再露出红圈类型的蓝天空腔；默认值恢复为 `3px` 后运动帧持续变化，暂停帧保持一致，控制台无警告或错误。

## 日间树风动接入 Day Scene Studio

- `?studio=day` 的树层新增 `1672 × 941` Canvas，并接入实验页确认的内部承托叶影 → 后层深绿叶 → 固定树干与主枝 → 前层亮叶和枝梢绘制顺序；四层加载完成前继续显示 `tree-day-v2` 静态树作为兜底。
- Day Studio 停止轮播旧整树 PNG，`Strength` 直接控制 `1–3px` 树冠最大摆幅；默认保持 `3px`、`8px` 前层条带、`1px` 接缝护边、`3.6s` 循环和每秒 12 次 Canvas 重绘。草地帧、场景坐标、缩放与深度控制维持原值。
- `prefers-reduced-motion: reduce` 时 Canvas 绘制零偏移静态帧；Night Studio 继续使用原夜树帧，公开日间主页继续使用原始静态树。

## Studio 接入验证

- 新增 Day Studio 接入回归测试，锁定 Canvas 尺寸、四层素材、绘制顺序、风力参数、降动效路径和旧整树帧隔离；`npm test`：121 项全部通过。
- 本地浏览器验证 `Strength` 的 Gentle / Gusty 实时切换及默认值恢复；Day Studio 树冠连续运动，Night Studio 仍显示夜间帧，公开日间主页仍显示 `tree-day-v2.png` 且无整树位移，三个页面控制台均无警告或错误。

## Day Studio 风动参数同步

- Day Scene Studio 的单项 `Strength` 已替换为实验页确认的三项参数：最大摆幅 `0–6px`、默认 `3px`；条带高度 `4–32px`、默认 `8px`；循环时长 `2–7s`、默认 `3.6s`。
- 控制面板加入“显示遮盖层级”、由后到前的绘制顺序图例和树冠暂停按钮；预览颜色继续使用青色后叶、橙色固定枝干和黄绿色前叶，内部承托叶影维持最底层。
- 数值与布尔参数沿用 `zhengji-scene-studio-day-v3` 草稿保存；重新载入页面后继续使用最近一次设置。暂停只冻结树冠 Canvas，草地动画保持原有独立节奏。
- 修正 Day 风动控制组的主题隔离选择器，Night Scene Studio 不再受通用控制组样式覆盖。公开日间主页仍使用 `tree-day-v2` 静态树，Night Studio 保留当前夜树帧系统。

## 参数同步验证

- 本地浏览器在 `?studio=day` 下验证 `6px / 4px / 3.0s` 实时生效并在刷新后保留；遮盖层级颜色与绘制顺序正确，暂停后树冠连续截图一致，恢复后树冠继续变化，控制台无警告或错误。
- Night Scene Studio 中 Day 风动控制组保持隐藏；完整 `npm test`：122 项全部通过。

## 日间空气层实验预览

- 新增仅在 `?theme=day&qa=day-air` 开启的主页空气层：三条稀疏像素风痕与四个低密度花粉粒子沿树冠上方轻轻掠过，使用 `transform`、`opacity` 和 `steps()` 保持像素节奏；默认日间主页构图不变。
- 实验层随 `prefers-reduced-motion: reduce` 清空，移除 `qa=day-air` 即可回到当前稳定版日间主页。

## 日间空气层来源修正

- 将实验粒子的来源改为树冠内四个固定锚点，落叶沿右下方向离开树冠；每片叶子的水平、垂直位移均使用响应式 `clamp()`，避免落点随视口突然跳变。
- 移除悬空的独立花粉轨迹，保留极淡的分离像素风痕作为叶片经过的空气提示；叶片和风痕都遵循 `prefers-reduced-motion` 关闭规则。

## 日间空气层坐标修正

- 将 `home-day-air` 放入日间树的变换容器，使落叶锚点与树的平移、缩放和全屏焦点裁切保持同一坐标系；起点不再落在整张场景画布的左上区域。
- 新增回归测试，锁定空气层必须嵌套在 `home-scene-tree-wrap home-scene-day-only` 内。

## 日间空气层密度微调

- 预览层增加两片树冠落叶，动态叶片由四片调整为六片；像素叶片由约 `4×3px` 增至 `5×4px`，仍保持 `qa=day-air` 可选启用和低动态模式关闭。

## 夜树枝叶交界空腔修正

- 夜树继续使用现有 `tree-sway-v2` 八帧 PNG 与原位移轨迹，没有接入日景 Canvas。问题来自生成器仅为四周连续 `5 × 5` 均有不透明像素的叶片生成承托层，紧邻固定枝干的叶缘因此没有深色底层；移动后，每帧会在树冠中央露出 `706–1,016` 个透明夜空像素。
- 承托叶影扩展到以 `(1120, 360)` 为中心、半径 `360 × 220` 的树冠内部椭圆，并与原有安全内区合并；承托色继续按夜树像素的 `0.24R / 0.38G / 0.50B` 压暗，保留原像素透明度。
- 重新生成 `tree-sway-v2-00.png` 至 `07.png`；第 1、5 帧仍逐像素等于批准静止帧，树干、根系、风向、摆幅、八帧顺序和公开夜景节奏均保持原值。

## 夜树空腔修正验证

- 新增回归测试，逐帧审计中央树冠椭圆：六个移动帧暴露夜空的像素数均为 `0`；`tests/tree-wind-lab.test.mjs`：7 项全部通过。
- Night Scene Studio 逐帧检查 00–07，公开 `?theme=night` 另行复查；枝干周围由深蓝叶影连续承接，两处控制台均无警告或错误。
- 完整 `npm test`：124 项全部通过。

## Day Studio 定稿风场与草地连续性

- 将用户确认的 `6px` 最大摆幅、`4px` 条带高度和 `3.0s` 循环写为 Day Scene Studio 的初始值与恢复值；本地草稿键升级为 `zhengji-scene-studio-day-v4`，旧草稿不会覆盖这组定稿参数。
- 日间 Studio 草地停止轮播节奏不均的八帧 PNG，改用 `meadow-day-v1.png` 实时绘制 `1672 × 941` Canvas。`y = 680–841` 按整数像素条带随同一风场移动，`y = 842` 以下保持原图固定，草根和土层不参与摆动。
- 草地以每秒 12 次重绘，相邻条带偏移最多相差 `1px`，上下各保留 `1px` 接缝护边；循环首尾回到零偏移。日间隐藏已经失效的“Motion / frame”滑杆，Night Studio 继续保留夜草帧速控制。
- `prefers-reduced-motion: reduce` 下日间草地回到零偏移静态帧。公开白天首页继续使用已批准的 `meadow-day-wind-v7` 与 `180ms` 节奏，待 Studio 版本确认后再接入公开页面。

## 日间草地验证

- 新增风场单元测试与 Studio 接入断言，覆盖 `3.0s` 闭环、整数偏移、`6px` 上限、相邻条带连续性、护边重叠、固定草根、12fps、低动态路径和日夜控制隔离。
- 本地浏览器在 `?studio=day` 连续抽查三个相位：草尖持续移动，草坪下半部稳定，参数显示 `6px / 4px / 3.0s`，控制台无警告或错误。
- 完整 `npm test`：127 项全部通过。
