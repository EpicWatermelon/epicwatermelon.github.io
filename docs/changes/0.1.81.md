# 0.1.81

- 日期：2026-08-02
- 状态：本地工作树
- 当前版本来源：`package.json`

## 白天场景默认组合

- 将确认的 Day Scene Studio 组合写入默认值：树、地基、草地、层级和金色树冠灯光保持用户给定的坐标与比例。
- 公开白天首屏和 Day Scene Studio 默认使用第 3 档强风帧组：`tree-day-wind-v9`、`meadow-day-wind-v7`，帧节奏设为 `180ms`。
- Day Studio 使用新的本地草稿键 `zhengji-scene-studio-day-v3`，避免浏览器中的旧 `Breezy / 260ms` 草稿覆盖定稿。
- CSS、i18n 与动态脚本缓存参数升级至 `0.1.81`，本地预览刷新后加载新配置。

## 验证

- `npm test`：114 项全部通过。
- `?studio=day` 刷新后显示 `Gusty` 与 `180ms`，并加载强风树冠和草地帧组。

## 日景树冠连续性

- 日景主页改用单张 `tree-day-v2` 树图，以 `home-day-tree-sway` 在 1440ms 内进行 `0px → 3px → 1px → 0px` 的整数像素摆动。移除完整静态底图和逐帧树冠的叠加，消除树冠边缘的拖影与蓝天间隙。
- 新增回归测试，锁定日景树的单层渲染和离散像素位移。
