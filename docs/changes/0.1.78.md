# 0.1.78

- 日期：2026-07-31
- 状态：本地工作树
- 当前版本来源：`package.json`

## 马犬尾根像素修复

- 发现 `malinois-base-v1.png` 的尾巴遮罩使用软 alpha 相减，尾根仍会与动画尾巴共享少量半透明边缘像素。
- 构建脚本现在将尾巴联合遮罩二值化后再挖空底图，并输出全新的 `malinois-base-v2.png`，避免客户端复用之前的中间资产。
- Day Scene Studio 切换到 `malinois-base-v2.png`；口部八帧和尾巴八帧保持原锚点不变。
- 像素级回归测试覆盖八个时刻，确认底图、尾巴与口部的重合非透明像素均为零。

## 白天公开场景

- 00 白天场景暂时撤下 Malinois，只保留天空、单色像素树叶、风动树冠、地基与草地。
- Malinois 的素材、分层动画和 Day Scene Studio 调试入口完整保留，后续可在确定构图后重新启用。

## 缓存与验证

- 页面 CSS、i18n 与动态脚本缓存参数升级到 `0.1.78`。
- `npm test` 共 112 项全部通过。
- 已在 1619×898 视口检查强制白天主页：公开 Malinois 图层数量为 0，保留 24 片动态像素树叶，页面无横向溢出和控制台错误。
