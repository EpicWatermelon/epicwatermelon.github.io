# 0.1.76

- 日期：2026-07-31
- 状态：本地工作树
- 当前版本来源：`package.json`

## 马犬分层修复

- 移除 body 旧切片中残留的尾巴像素，避免静态尾巴与 Image2 动画尾巴重复叠放。
- 头部动画改为 Image2 生成的头颈专用八帧；脚本只替换口鼻、下颌和舌头区域，身体、尾巴、耳朵和眼睛保持锚点固定。
- 新增 `malinois-head-pant-v8-00.png` 至 `-07.png`，并通过 `dynamic-cv.js?v=0.1.76` 强制本地 Studio 获取新图层逻辑。

## 验证

- 先运行缓存版本测试，确认 `0.1.75` 会失败；升级后通过。
- `npm test`：107 项通过。
- 重载 Day Studio 后确认实际资源为 `malinois-body-v5.png`、`malinois-tail-v5-*.png` 与 `malinois-head-pant-v8-*.png`。

## Background 舰队资产修正

- 保留月级巡洋舰资产；将休伯利安号替换为 `hyperion-background-fleet-v2.png`。
- 新精灵以泰伦战列巡洋舰的后置圆形推进器、宽大舰体、分离侧翼炮组及上置主炮为识别重点，移除旧版小型炮艇轮廓。
- 横向飞行触发、时长与无障碍降动效规则保持不变。
