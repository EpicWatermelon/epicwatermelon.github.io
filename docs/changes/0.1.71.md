# 0.1.71

- 日期：2026-07-31
- 状态：本地工作树
- 当前版本来源：`package.json`

## Contact 语言边界

- 文字彩蛋仅在中文版 `zh-CN` 启用。
- 英文版使用普通鼠标样式，隐藏进度星，点击不累计，不触发动效，键盘状态标记为不可用。
- 从英文切换到中文后立即启用；从中文切回英文时重置未完成的点击进度并恢复英文标题。
- 长期规则同步记录在 [`../design/INTERACTION_RULES.md`](../design/INTERACTION_RULES.md)。

## 文档与版本号

- 文档变更记录从日期文件改为 `changes/<version>.md`，文件名与 `package.json` 当前版本一致。
- 新建 [`../CHANGELOG.md`](../CHANGELOG.md)，按版本倒序登记 `0.1.65`–`0.1.71` 的状态和详细记录。
- `0.1.65` 明确关联 Git 提交 `5b74db9`；`0.1.66`–`0.1.71` 明确标记为本地工作树，避免混淆提交历史。
- `AGENTS.md` 要求每次修改完成后更新当前版本记录；新版本同时登记到总索引。
- 清理 ADR、偏好和 TODO 中把 `0.1.38` 当作当前版本的过期描述。
- Markdown 文档进入版本控制；仓库恢复用的 `docs/*.bundle` 继续保持本地并由 `.gitignore` 排除。

## 验证

- 自动化测试检查 Contact 彩蛋的中英文边界、版本索引、七个版本文件和文档治理规则。
- `npm test`：102 项全部通过。
