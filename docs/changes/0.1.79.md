# 0.1.79

- 日期：2026-07-31
- 状态：本地工作树
- 当前版本来源：`package.json`

## 白天绿色主题

- 01–05 的白天运行时色板从雾白与暖纸色改为鼠尾草绿、苔绿、薄荷绿和灰青绿，并继续通过滚动插值平滑过渡。
- 白天页面底色改为 `#9fbea0`；Bio 使用 `#a8c8a3`，Background 使用 `#aebf91`，Ophthalmic 使用 `#98c3a6`，Industrial 使用 `#91b8a4`，Contact 使用 `#a5bd93`。
- 教育面板、履历入口卡、项目卡和联系按钮全部改用半透明绿色表面，取消白色纸张感。
- 标题、正文、导航、小字号说明和页脚统一改为深森林绿；标题基准色为 `#102f23`，正文基准色为 `#163a2b`，辅助文字为 `#2c4938`。
- 00 白天首页继续保留蓝天与绿色像素树构图，进入 Bio 时过渡到绿色页面底色。

## 缓存与验证

- 页面 CSS、i18n 与动态脚本缓存参数升级到 `0.1.79`，浏览器白天主题色同步改为 `#9fbea0`。
- 桌面端检查 Bio、Background 与 Contact，375×812 检查 Industrial；页面均无横向溢出。
- 标题对 Contact 基础绿色的对比度为 7.09:1，辅助正文最低目标保持在 4.5:1 以上。
- `npm test` 共 112 项全部通过。

## Background 战列舰资产修正

- 保留月级巡洋舰资产，将休伯利安号替换为 `hyperion-background-fleet-v3.png`。
- 新精灵按确认参考的斜俯视四臂轮廓制作，保留中央斜脊、左下环形大型舱体、四组外伸武器模块、蓝色装甲嵌板和琥珀灯光。
- 舰队彩蛋的出现条件、横向飞行时长与降动效规则保持不变。

## 本次验证

- 战列舰舰队专项测试通过；本地静态预览可直接加载 `hyperion-background-fleet-v3.png`。
- 全量 `npm test` 当前为 109 项通过、1 项失败。失败断言仍要求白天马犬的 `malinois-tail` 八帧配置，而当前脚本已移除该配置；该项与本次舰队资产无关，未改动。
