# 0.1.73

- 日期：2026-07-31
- 状态：本地工作树
- 当前版本来源：`package.json`

## Saber 音乐播放范围

- 《孤独な巡礼》开始播放后跨章节持续播放，滚动离开 00 不再暂停。
- 删除对 `data-active-chapter` 的音乐暂停监听。
- 已解锁后再次点击 Saber 仍会暂停；暂停后再次点击继续播放。
- 标签页隐藏或页面离开时继续暂停，歌曲自然结束后可点击 Saber 重播。

## 验证

- 自动化测试检查跨章节播放规则、点击暂停入口和页面生命周期处理。
- 真实浏览器中从 00 播放后跳到 01 / Bio，`activeChapter` 变为 `bio` 时音频仍继续播放；返回 00 点击 Saber 后正常暂停并同步 `aria-pressed="false"`。
- 页面控制台无报错。
- `npm test`：音乐与其余 102 项通过；Malinois 喘气动画测试因工作区缺少 `assets/pixel/home/malinois-head-pant-v7-00.png` 而失败，与本版本音乐修改无关。
